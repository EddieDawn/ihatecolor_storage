# Project Instructions

## UI 디자인 가드레일

- UI, 레이아웃, 스타일, 타이포그래피, 이미지 표현, 애니메이션을 생성하거나 수정하기 전에 반드시 `docs/DESIGN_SYSTEM.md`를 처음부터 끝까지 읽는다.
- `docs/DESIGN_SYSTEM.md`를 이 프로젝트의 디자인 기준에 대한 단일 진실 공급원(source of truth)으로 취급한다.
- 사용자가 명시적으로 승인하지 않은 상태에서 `docs/DESIGN_SYSTEM.md`의 토큰, 규칙, 허용 범위 또는 변경 이력을 수정하지 않는다.
- 구현상 디자인 시스템과 다른 처리가 필요하거나 더 낫다고 판단되면 먼저 사용자에게 다음 중 무엇인지 묻고 답을 기다린다.
  1. 해당 화면만의 예외인지
  2. 디자인 시스템 전체를 변경할 것인지
- 사용자의 최신 프롬프트가 기존 디자인 시스템과 충돌하면 최신 프롬프트를 우선하되, 구현 전에 `docs/DESIGN_SYSTEM.md`도 함께 갱신할지 질문한다.
- 승인된 변경은 문서에 먼저 반영한 후 코드에 적용한다.
- 새 스타일 값은 임의의 매직 넘버로 흩어 놓지 말고, 가능한 경우 문서에 정의된 CSS 토큰을 사용한다.

## 승인된 화면 역할

- 랜딩 페이지의 시각적 기준은 `prototype-c/`이다.
- 사진 목록 페이지의 시각적 기준은 `prototype-b/`이다.
- 사진 목록은 데스크톱에서 3열 카드와 명시적 페이지네이션을 사용한다. 무한 스크롤로 대체하지 않는다.
- `prototype-b/`와 `prototype-c/`는 비교 기준이므로 사용자가 명시적으로 요청하지 않는 한 수정하지 않는다.

## 검증

- 데스크톱 UI는 우선 1440×900에서 시각적으로 검증한다.
- 디자인 작업을 완료했다고 보고하기 전에 색상, 폰트 스택, 좌우 여백, 열 수, 이미지 비율, 캡션, 호버 및 모션이 `docs/DESIGN_SYSTEM.md`와 일치하는지 확인한다.

